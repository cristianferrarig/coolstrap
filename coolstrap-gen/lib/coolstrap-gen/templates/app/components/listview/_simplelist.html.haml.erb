-# more on lists in http://
  
= article({:class=>"scrollable"}) do

  = list_view do 
  
    = divider do
      divider

    = list_item() do
      normal item
    
    = divider(:id=>"divider1") do
      2. Simple with id 
             
    = list_item(:arrow=>"left") do
      .content
        %p simple content with left arrow.

    = list_item(:arrow=>"right") do
      .content
        %p simple content with right arrow.    